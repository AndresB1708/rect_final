export function cotizar(valor, propiedad, ubicacion, metros){
        let resultado = (valor * propiedad * ubicacion * metros)
            return resultado.toFixed(2)
            //console.log(resultado);


    
}